PyPaint
PythonとPySide6を使用した、画像処理エフェクト機能付きのペイントアプリケーションです。 通常の描画機能に加え、画像の回転やぼかしをリアルタイムで適用しながら、その上から正確に描画を続けることが可能です。

🚀 主な機能
ペイント機能

自由線描画（ブラシ）

Shiftキー + ドラッグ による直線描画

消しゴムモード（現在の背景色に適応）

ブラシサイズ・色のカスタマイズ

画像処理エフェクト (OpenCV)

リアルタイム回転（-180° 〜 180°）

ガウスぼかし（リアルタイム調整）

背景色の動的変更

座標変換ロジック

画像を回転させた状態でも、マウスの位置を正確に元の画像座標へ逆変換して描画する高度な計算処理を実装。

ファイル操作

既存画像の読み込み（PNG/JPG/JPEG）

🛠 使用技術
Python 3.x

PySide6: GUIフレームワーク

OpenCV (opencv-python): 画像処理・座標変換

NumPy: 画像データ（行列）操作

📦 セットアップ・実行方法
ライブラリのインストール

Bash
pip install PySide6 opencv-python numpy
アプリケーションの起動

Bash
python main.py
💡 使い方
描画: キャンバス上を左クリックでドラッグします。

直線: Shift キーを押しながらドラッグすることで、始点から終点まで直線を引けます。

背景変更: 「背景色を変える」ボタンからキャンバス全体の色を変更できます（消しゴムの色も自動追従します）。

エフェクト: 右側のスライダーで「回転」や「ぼかし」を調整します。回転したまま描き込むことも可能です。